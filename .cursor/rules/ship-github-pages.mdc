---
description: Deprecated pointer — use auto-ship-and-migrations.mdc
alwaysApply: true
---

# Ship + migrations

All shipping and migration steps live in **`auto-ship-and-migrations.mdc`**.

Quick reference: `npm run ship -- "message"` → validate, `db:push:remote`, commit, `git push origin main` → GitHub Pages deploy.

Never ask the user to push or run migrations.
